<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alex Chen · clean portfolio</title>
    <!-- CSS (no JS) – dark gradient theme, modern, responsive -->
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: #0c0f15; /* deep base */
            font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', sans-serif;
            line-height: 1.6;
            color: #e0e4ed;
            scroll-behavior: smooth;
        }

        /* gradient background wrapper (subtle dark gradient) */
        .gradient-bg {
            background: radial-gradient(circle at 30% 10%, #1e2635 0%, #0b0e14 80%);
            min-height: 100vh;
        }

        /* main container – clean width, centered */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem 1.5rem;
        }

        /* ===== hero section ===== */
        .hero {
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            padding: 3rem 0.5rem 4rem;
        }

        /* circular profile image placeholder – easy replace */
        .profile-img {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid rgba(255, 255, 255, 0.08);
            background: linear-gradient(145deg, #2d374f, #1e2638);
            box-shadow: 0 20px 30px -10px rgba(0, 0, 0, 0.7), 0 0 0 1px rgba(120, 180, 255, 0.15) inset;
            margin-bottom: 2rem;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            /* placeholder image via css (will be overwritten if user adds <img> inside) */
            display: flex;
            align-items: center;
            justify-content: center;
            color: #a0b5d4;
            font-size: 0.9rem;
            background-image: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" fill="%233d4b66"><circle cx="50" cy="45" r="22"/><circle cx="50" cy="85" r="30"/></svg>');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
        }

        /* if user adds img tag inside, it will override background (keep circular) */
        .profile-img img {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            object-fit: cover;
            display: block;
        }

        .profile-img:hover {
            transform: scale(1.02);
            box-shadow: 0 25px 40px -8px #000d1f, 0 0 0 2px rgba(140, 200, 255, 0.4) inset;
        }

        .hero h1 {
            font-size: 3rem;
            font-weight: 600;
            letter-spacing: -0.02em;
            background: linear-gradient(130deg, #ffffff, #c0d0f0);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            margin-bottom: 0.6rem;
            line-height: 1.2;
        }

        .hero .subhead {
            font-size: 1.4rem;
            font-weight: 350;
            color: #b1c3df;
            max-width: 600px;
            margin: 0.5rem auto 0;
            opacity: 0.9;
        }

        /* ===== section headings ===== */
        .section-title {
            font-size: 2.3rem;
            font-weight: 500;
            text-align: center;
            margin: 3rem 0 2.5rem;
            background: linear-gradient(145deg, #f0f5ff, #9db8f0);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            letter-spacing: -0.01em;
        }

        /* ===== about section ===== */
        .about-text {
            max-width: 850px;
            margin: 0 auto;
            background: rgba(18, 24, 36, 0.5);
            backdrop-filter: blur(4px);
            -webkit-backdrop-filter: blur(4px);
            border-radius: 36px;
            padding: 2.2rem 2.5rem;
            border: 1px solid rgba(80, 120, 200, 0.15);
            box-shadow: 0 20px 35px -15px rgba(0, 0, 0, 0.7);
            font-size: 1.2rem;
            color: #d0dcf0;
            line-height: 1.7;
            transition: border 0.2s;
        }

        .about-text:hover {
            border-color: rgba(100, 160, 250, 0.3);
        }

        .about-text p + p {
            margin-top: 1.5rem;
        }

        /* ===== projects grid ===== */
        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
            margin-top: 1rem;
        }

        .project-card {
            background: rgba(16, 22, 34, 0.7);
            backdrop-filter: blur(3px);
            -webkit-backdrop-filter: blur(3px);
            border: 1px solid rgba(70, 120, 210, 0.2);
            border-radius: 30px;
            padding: 2rem 1.8rem 2rem;
            transition: transform 0.2s ease, border 0.3s, box-shadow 0.3s;
            box-shadow: 0 20px 25px -15px #03060b;
        }

        .project-card:hover {
            transform: translateY(-8px);
            border-color: #4f7fd6;
            box-shadow: 0 30px 35px -12px #04080f, 0 0 0 1px rgba(100, 160, 250, 0.3);
        }

        .project-card h3 {
            font-size: 1.9rem;
            font-weight: 500;
            margin-bottom: 1rem;
            background: linear-gradient(120deg, #dfe9ff, #aac0f0);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .project-card p {
            color: #b4c5e5;
            margin-bottom: 1.8rem;
            font-size: 1.1rem;
        }

        .project-tag {
            display: inline-block;
            background: rgba(45, 70, 130, 0.4);
            border-radius: 50px;
            padding: 0.4rem 1.3rem;
            font-size: 0.95rem;
            font-weight: 450;
            color: #ccdcff;
            border: 1px solid rgba(80, 140, 240, 0.5);
            transition: background 0.2s;
        }

        .project-card:hover .project-tag {
            background: rgba(55, 95, 180, 0.5);
            border-color: #7ca5ff;
        }

        /* ===== skills section ===== */
        .skills-list {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 1.2rem 1.5rem;
            max-width: 750px;
            margin: 2.5rem auto 0;
        }

        .skill-item {
            background: rgba(30, 40, 60, 0.7);
            backdrop-filter: blur(2px);
            border: 1px solid #2e3f60;
            padding: 0.9rem 2rem 0.9rem 2rem;
            border-radius: 60px;
            font-size: 1.3rem;
            font-weight: 450;
            color: #e7edff;
            display: flex;
            align-items: center;
            gap: 0.7rem;
            transition: all 0.2s;
            box-shadow: 0 5px 10px rgba(0, 0, 0, 0.3);
        }

        .skill-item:hover {
            background: #1f2f4a;
            border-color: #5f8aff;
            transform: scale(1.03);
            color: white;
            box-shadow: 0 10px 18px -5px #020812;
        }

        /* skill icon (unicode / simple font icons) */
        .skill-icon {
            font-size: 1.6rem;
            filter: drop-shadow(0 2px 4px #1e2f4a);
        }

        /* ===== contact section ===== */
        .contact-block {
            background: rgba(10, 16, 26, 0.6);
            backdrop-filter: blur(4px);
            border: 1px solid #2e3e60;
            border-radius: 60px;
            padding: 2.8rem 2.5rem;
            max-width: 700px;
            margin: 2.5rem auto 1.5rem;
            text-align: center;
        }

        .contact-email {
            font-size: 1.8rem;
            font-weight: 400;
            background: linear-gradient(145deg, #f0f5ff, #b3ceff);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            margin-bottom: 1.8rem;
            display: inline-block;
            border-bottom: 2px dashed rgba(90, 160, 255, 0.4);
            padding-bottom: 6px;
            transition: border 0.2s;
        }

        .contact-email:hover {
            border-bottom-color: #8bb2ff;
        }

        .social-links {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 2rem;
            margin-top: 1rem;
        }

        .social-link {
            color: #c1d3f5;
            text-decoration: none;
            font-size: 1.5rem;
            font-weight: 400;
            display: inline-flex;
            align-items: center;
            gap: 0.4rem;
            padding: 0.6rem 1.2rem;
            border-radius: 80px;
            background: rgba(28, 40, 60, 0.7);
            border: 1px solid #31486c;
            transition: all 0.2s;
        }

        .social-link:hover {
            background: #253c60;
            border-color: #6093ff;
            color: white;
            transform: translateY(-3px);
        }

        /* simple icon placeholders (use text/emoji for simplicity) */
        .icon {
            font-size: 1.8rem;
            line-height: 1;
        }

        /* footer small */
        .foot-note {
            text-align: center;
            margin-top: 4.5rem;
            color: #62749e;
            font-size: 0.95rem;
            border-top: 1px solid #1f2c41;
            padding-top: 2rem;
        }

        /* responsive fine-tuning */
        @media (max-width: 650px) {
            .hero h1 {
                font-size: 2.4rem;
            }
            .hero .subhead {
                font-size: 1.2rem;
            }
            .section-title {
                font-size: 2rem;
            }
            .about-text {
                padding: 1.8rem 1.5rem;
                font-size: 1.1rem;
            }
            .project-card {
                padding: 1.8rem 1.5rem;
            }
            .skill-item {
                font-size: 1.2rem;
                padding: 0.7rem 1.5rem;
            }
            .contact-email {
                font-size: 1.4rem;
                word-break: break-all;
            }
            .social-link {
                font-size: 1.2rem;
                padding: 0.5rem 1rem;
            }
        }

        @media (max-width: 450px) {
            .profile-img {
                width: 120px;
                height: 120px;
            }
            .hero h1 {
                font-size: 2rem;
            }
            .projects-grid {
                grid-template-columns: 1fr;
            }
            .social-links {
                gap: 1rem;
            }
        }

        /* remove default link underline, smooth */
        a {
            text-decoration: none;
        }

        /* subtle hover for any interactive */
        .project-card, .social-link, .skill-item {
            cursor: default;
        }

        /* placeholder note: profile image area is pure css but accepts img replacement */
    </style>
</head>
<body>
    <div class="gradient-bg">
        <div class="container">
            <!-- hero section with circular profile placeholder -->
            <section class="hero">
                <div class="profile-img">
                    <!-- 
                        =============================================
                        EASY REPLACE: put your own image here 
                        just add an <img> tag inside this div.
                        Example: 
                        <img src="your-photo.jpg" alt="Your Name">
                        =============================================
                    -->
                    <!-- <img src="profile.jpg" alt="Your Name"> --> 
                </div>
                <h1>Alex Chen</h1>
                <div class="subhead">design & code · creative developer</div>
            </section>

            <!-- about me -->
            <section>
                <h2 class="section-title">about me</h2>
                <div class="about-text">
                    <p>I'm a developer and designer with a passion for building clean, human-friendly interfaces. Over the past 6 years I’ve collaborated with startups and agencies to craft digital experiences that feel both minimal and alive. I believe in the power of gradient light and dark space.</p>
                    <p>Based in Seattle, I enjoy working at the intersection of frontend architecture, visual design, and performance. Outside of coding, I mentor at local hackathons and collect vintage synthesizers.</p>
                </div>
            </section>

            <!-- projects section with cards -->
            <section>
                <h2 class="section-title">selected projects</h2>
                <div class="projects-grid">
                    <div class="project-card">
                        <h3>nimbus UI</h3>
                        <p>Component library with dark gradient foundations, used by 200+ designers. Accessible and flexible.</p>
                        <span class="project-tag">design system</span>
                    </div>
                    <div class="project-card">
                        <h3>orbit finance</h3>
                        <p>Analytics dashboard for personal finance – smooth animations and deep‑space theme.</p>
                        <span class="project-tag">react / d3</span>
                    </div>
                    <div class="project-card">
                        <h3>drift CMS</h3>
                        <p>Lightweight, file‑based CMS for static sites. Focused on developer joy and clean editing.</p>
                        <span class="project-tag">node.js</span>
                    </div>
                    <div class="project-card">
                        <h3>eclipse sound</h3>
                        <p>Interactive audio visualizer with Web Audio API and canvas. Real‑time starfield.</p>
                        <span class="project-tag">web audio</span>
                    </div>
                </div>
            </section>

            <!-- skills with icons (styled list) -->
            <section>
                <h2 class="section-title">tools & skills</h2>
                <div class="skills-list">
                    <div class="skill-item"><span class="skill-icon">⚛️</span> React</div>
                    <div class="skill-item"><span class="skill-icon">🎨</span> Figma</div>
                    <div class="skill-item"><span class="skill-icon">⚡</span> Vue</div>
                    <div class="skill-item"><span class="skill-icon">📱</span> Flutter</div>
                    <div class="skill-item"><span class="skill-icon">🖥️</span> Node.js</div>
                    <div class="skill-item"><span class="skill-icon">🌈</span> CSS / SCSS</div>
                    <div class="skill-item"><span class="skill-icon">🛠️</span> Python</div>
                    <div class="skill-item"><span class="skill-icon">📐</span> UI/UX</div>
                </div>
                <!-- small note: icons are unicode / text for simplicity, but feel like icons -->
            </section>

            <!-- contact & social links -->
            <section>
                <h2 class="section-title">connect</h2>
                <div class="contact-block">
                    <a href="mailto:alex.chen@example.dev" class="contact-email">alex.chen@example.dev</a>
                    <div class="social-links">
                        <a href="#" class="social-link"><span class="icon">📬</span> github</a>
                        <a href="#" class="social-link"><span class="icon">🔗</span> linkedin</a>
                        <a href="#" class="social-link"><span class="icon">📷</span> instagram</a>
                        <a href="#" class="social-link"><span class="icon">🐦</span> x</a>
                    </div>
                </div>
            </section>

            <!-- subtle footer -->
            <div class="foot-note">
                ✦ designed with minimalism & dark gradient ✦
            </div>
        </div>
    </div>

    <!-- no JavaScript — pure CSS magic -->
</body>
</html>
